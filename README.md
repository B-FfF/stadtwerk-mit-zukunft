# stadtwerk-mit-zukunft

## Set up local development

0. Prerequisite: ruby (i.e. as per https://jekyllrb.com/docs/installation/other-linux/)
1. Install Gems: `bundle` (same as `bundle install`)
2. Run: `bundle exec jekyll serve`

